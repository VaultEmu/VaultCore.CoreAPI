namespace VaultCore.CoreAPI;

//A Subsystem is a class that provide access to some functionality in the frontend. The Core will get passed in
//a class that implements ISubsystemResolver to allow the Core to get a reference to the subsystems it needs.
public interface ISubsystem 
{
    
}